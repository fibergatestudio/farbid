<div class="feature-icon feature4"></div>
<div class="feature-detail">
    <div class="ser-title">{!! $item->title !!}</div>
    @if($item->sub_title)
        <div class="ser-subtitle">
            {!! $item->sub_title !!}
        </div>
    @endif
</div>
